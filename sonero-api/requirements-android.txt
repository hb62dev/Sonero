# Requirements para Android (serious_python / ARM64)
# Sin sounddevice/pyaudiowpatch (grabación la hace Flutter nativamente)
# Sin shazamio (reemplazado por recognizer.py pure Python con httpx)

fastapi>=0.111.0
uvicorn>=0.29.0
python-multipart>=0.0.9
pydantic>=2.0.0
pydantic-settings>=2.0.0
aiofiles>=23.0.0

# Shazam: reemplazado por implementación pure Python en services/recognizer.py
# Solo necesitamos pydub para decodificar audio
pydub>=0.25.1

# YouTube download
yt-dlp>=2024.1.0

# Utils
python-dotenv>=1.0.0

# Metadata
mutagen>=1.47.0
pillow>=10.0.0

# Database
sqlalchemy>=2.0.0

# HTTP Client (usado por recognizer.py para llamar Shazam API)
httpx>=0.25.0
